# Shortcode Command 使用说明

Shortcode 用于在 `data.js` 中控制文本排版，无需直接编写复杂的 HTML。

## 基本语法

```text
{{命令: 内容}}
```

无须填写内容的命令直接写为：

```text
{{divider}}
```

Shortcode 可用于 `profile.heading`、`profile.bio` 和笔记的 `content` 字段。

## 命令列表

### 标题

```text
{{h1: 一级标题}}
{{h2: 二级标题}}
{{h3: 三级标题}}
```

`h1`、`h2`、`h3` 分别生成大、中、小三级标题。

### 加粗

```text
{{b: 需要加粗的文字}}
```

### 斜体

```text
{{i: 需要使用斜体的文字}}
```

### 引用块

```text
{{quote: 这是一段引用文字。}}
```

引用内容会显示左侧竖线、缩进和浅色背景。

### 居中

```text
{{center: 需要居中的文字}}
```

### 分割线

```text
{{divider}}
```

### 图片

```text
{{img: fig/example.png}}
```

图片路径相对于网站根目录填写。图片会自动居中，并带有圆角和阴影。

### 图片说明

```text
{{caption: 图：这里填写图片说明}}
```

通常紧接在 `img` 命令之后使用。

## 完整示例

```js
content: `
{{h2: 从夸父逐日窥见历法}}

这是一段普通正文，其中的{{b: 重点内容}}会被加粗。

{{quote: 这是一段引用。}}

{{img: fig/fuxi.png}}
{{caption: 图：古人利用圭表测影定节气}}

{{divider}}

{{center: 正文结束}}
`
```

## 使用注意事项

1. 一个 Shortcode 尽量写在同一行内。
2. 不要在一个 Shortcode 内嵌套另一个 Shortcode。
3. 命令名称区分大小写，请使用小写。
4. 图片路径错误时，图片将无法显示。
5. 首页笔记摘要会自动移除排版命令，只保留文字。
6. `profile.heading` 可以填写普通文本，也可以使用标题命令。